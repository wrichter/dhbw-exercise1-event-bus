kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: event-bus
spec:
  path: /
  to:
    kind: Service
    name: event-bus
    weight: 100
  port:
    targetPort: 4005-tcp
  wildcardPolicy: None